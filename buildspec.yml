version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto25
    commands:
      - echo Installing Maven...
      - java -version
  build:
    commands:
      - java -version
      - mvn clean package
